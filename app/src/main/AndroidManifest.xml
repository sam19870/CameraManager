<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- 读取当前 WiFi SSID 用于内网/穿透自动选路（Android 12+ 强制精确定位） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Local network discovery -->
    <uses-permission android:name="android.permission.QUERY_ALL_DEVICES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Storage / recording / screenshot -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Audio for voice intercom -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Camera (for local capture during intercom if needed) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Foreground service for streaming / detection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibrate for alarms -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock to keep preview alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Floating window preview -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".CameraApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.CameraManager">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.preview.PreviewActivity" android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.CameraManager.Preview"/>
        <activity android:name=".ui.scan.DeviceScanActivity" />
        <activity android:name=".ui.scan.AddDeviceActivity" />
        <activity android:name=".ui.playback.PlaybackActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.settings.DetectionRuleActivity" />
        <activity android:name=".ui.settings.AlarmLogActivity" />
        <activity android:name=".ui.voice.VoiceIntercomActivity" />
        <activity android:name=".ui.MultiPreviewActivity" />
        <activity android:name=".ui.PatrolActivity" />
        <activity android:name=".ui.manage.DeviceManageActivity" />
        <activity android:name=".ui.detection.DetectionRegionActivity" />
        <activity android:name=".ui.tunnel.TunnelManageActivity" />
        <activity android:name=".ui.settings.AppSettingsActivity" />
        <activity android:name=".ui.settings.LogCollectActivity" />
        <activity android:name=".ui.settings.DeviceControlActivity" />
        <activity android:name=".ui.settings.NightVisionActivity" />
        <activity android:name=".ui.settings.VideoAudioConfigActivity" />

        <service
            android:name=".service.CameraStreamService"
            android:foregroundServiceType="camera"
            android:exported="false" />

        <service
            android:name=".service.MotionDetectionService"
            android:foregroundServiceType="camera"
            android:exported="false" />

        <service
            android:name=".service.FloatingWindowService"
            android:foregroundServiceType="camera"
            android:exported="false" />

        <receiver
            android:name=".service.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.cameramanager.app.action.ALARM" />
                <action android:name="com.cameramanager.app.action.DISMISS_ALARM" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
